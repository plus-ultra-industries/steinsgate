name: DarkForge Policy Gate (Consumer)

on:
  pull_request:
    types: [opened, synchronize, reopened, labeled, unlabeled]

jobs:
  policy_gate:
    uses: plus-ultra-industries/darkforge/.github/workflows/policy-gate.yml@main
    secrets: inherit
